# Project Context

This is a Gemini CLI SubAgent project.

## Documentation Rules

1. **Location**: All system documentation must be stored in the `./docs` directory.
2. **Language**: All documentation inside `./docs` must be written in **Vietnamese**.
3. **Format**: Use Markdown for all documentation.
